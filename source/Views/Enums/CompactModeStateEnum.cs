namespace Unification.Views.Enums
{
    /// <summary>
    /// An enum for use with views that have compact layout toggles to denote view layout toggle state.
    /// </summary>
    public enum CompactModeState
    {
        Full    = 1,
        Off     = 0,
        Partial = 3
    }
}
